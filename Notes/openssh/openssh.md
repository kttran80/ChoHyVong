// http://stackoverflow.com/questions/13449525/sharpssh-invalid-privatekey
            // create a key with OpenSSH format
            // using PuttyGen to generate and convert the key (Conversion -> export OpenSSH Key)
            // checking the tool too